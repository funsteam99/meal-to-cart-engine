# Freshwise QA Record

This records what has been verified after Cloud Run revision `freshwise-00009-ghp`.

## Deployment

- Cloud Run service URL: `https://freshwise-539021301650.asia-east1.run.app`
- Previous URL still reachable: `https://freshwise-lyhoyhjnca-de.a.run.app`
- Both URLs returned `200 OK` after deployment.
- Latest verified revision: `freshwise-00009-ghp`
- Cloud Run traffic: 100% routed to the latest revision.
- Runtime model setting: `DEFAULT_MODEL=gemini-2.5-flash`
- Runtime API key source: Secret Manager `freshwise-google-api-key:latest`
- BigQuery analytics env:
  - `BIGQUERY_PROJECT_ID=subtle-fulcrum-496004-d5`
  - `BIGQUERY_DATASET=freshwise_analytics`
  - `BIGQUERY_EVENTS_TABLE=events`

## UX Verification

The deployed app was verified at the GCP URL after the shopper UX update:

- Meal-to-cart journey stepper appears above the main flow.
- Manual ingredient entry is presented as the primary fast path.
- Photo scan remains available as a secondary path.
- Recipe options show owned ingredient count, missing item count, recommended product count, and estimated basket amount.
- Cart page presents recommended products as missing items needed to complete the selected meal.
- Admin tab includes retailer-facing PoC analytics context.
- Traditional Chinese UI renders correctly on the deployed app.

## Gemini Verification

The deployed GCP app was tested with:

```text
tomatoes, spinach, eggs, cheese
```

Flow:

1. Open the deployed Cloud Run app.
2. Select `Use Demo Ingredients`.
3. Open `Recipe`.
4. Click `Regenerate Recipe`.

Result:

- The app returned `Generated by the configured model.`
- The generated recipe options differed from the local fallback recipes.
- The request used `gemini-2.5-flash` through the Cloud Run `DEFAULT_MODEL` env var.
- Local fallback remained available for unavailable or timed-out model calls.

## Analytics Flow Evidence

Recent BigQuery events confirmed the demo path has data for:

- `ingredients_updated`
- `recipe_generated`
- `recipe_selected`
- `product_recommended`
- `product_added_to_cart`
- `product_removed_from_cart`
- `checkout_started`
- `order_line_item`
- `order_completed`

The latest verified session included:

- 1 `recipe_generated`
- 1 `recipe_selected`
- 5 `product_recommended`
- 2 `product_added_to_cart`
- 2 `product_removed_from_cart`
- 5 `order_line_item`
- 1 `order_completed`
- mock cart subtotal: `$25.13`

The selected recipe event included:

- `recipe_index`: `2`
- meal title: `奶油番茄菠菜雞肉義大利麵`

## Admin Metrics Evidence

The same BigQuery logic used by the in-app Admin dashboard returned non-zero metrics:

- event count: `23`
- session count: `1`
- recipe count: `1`
- product recommendation count: `5`
- order count: `1`
- order line item count: `5`
- average mock cart subtotal: `$17.15`

This confirms the Admin dashboard has updated data available after the flow.

## Top Ingredients Normalization

The dashboard normalization logic was verified with:

```text
Eggs
eggs
 EGGS 
```

All three normalize to:

```text
Eggs
```

## Remaining Visual QA

Visual browser confirmation was provided by screenshot for:

- Traditional Chinese UI labels in the main app shell.
- Traditional Chinese cart tab.
- Mock order confirmation card after checkout.
- Confirmation card order metrics: item count, order total, order ID, and line items.

The full flow including recipe selection, cart quantity changes, checkout, order line items, order completion, and returnable Admin metrics is verified by BigQuery event order.
